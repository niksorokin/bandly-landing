V33 isolated follow-on from V32. V1–V32 and the live root remain unchanged.

Scope:
- Redraw the SVG lockup from the supplied reference with clean geometric mark shapes.
- Keep the BANDLY wordmark as path outlines and use the same SVG lockup in the header, footer, and workspace brand.
- Remove the hero duplicate so the logo appears once above the fold, in the header.
- Add a staged load animation for the squircle, plates, ember wedge, and wordmark, with a readable reduced-motion state.
